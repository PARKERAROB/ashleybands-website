# Charges model — what does a family owe?

type: wayfinder:grilling
status: open
assignee:
blocked-by:

## Question

The ledger needs the debit side defined: which charges exist (fair share, trip,
fees), their amounts, per-student vs per-family, who gets charged what (MB vs concert
only, guard), proration/waiver handling, and how existing PayPal + check records map
onto opening balances. Rob's call on every line; the two-bucket funding doctrine
(roles-context) frames it.
