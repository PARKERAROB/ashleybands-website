# Admin information architecture — prototype

type: wayfinder:prototype
status: open
assignee:
blocked-by: 012-schema-admin-coverage-audit.md

## Question

What does "Rob sees and edits everything" look like as screens? A cheap clickable
mock (deployed to prod behind admin auth — Rob reviews on the live site, never
localhost) of the admin's shape: navigation, one list+edit screen pattern, where
audit/provenance shows, where CSV export lives. Rob reacts, we lock the pattern, and
every domain's screen graduates from the fog as a build ticket using it.
