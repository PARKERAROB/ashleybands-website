import PortalRequestConfirmClient from "./PortalRequestConfirmClient";

export const metadata = {
  title: "Confirm Profile Request | Ashley Bands",
  description: "Confirm an Ashley Bands profile access request."
};

export default function PortalRequestConfirmPage() {
  return <PortalRequestConfirmClient />;
}
