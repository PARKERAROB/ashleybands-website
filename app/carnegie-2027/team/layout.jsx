export const dynamic = "force-dynamic";
export const metadata = {
  title: "Private Carnegie workspace | Ashley Bands",
  robots: { index: false, follow: false, archive: false },
};
export default function TeamLayout({ children }) {
  return children;
}
